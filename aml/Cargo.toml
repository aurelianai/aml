[package]
name = "aml"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "sgemm"
harness = false

[dependencies]
half = "2.3.1"

[dev-dependencies]
criterion = {version = "0.3", features = ["html_reports"]}
rand = "0.8.5"
